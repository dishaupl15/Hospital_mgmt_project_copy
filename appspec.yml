version: 0.0
os: linux

files:
  - source: agentic-health-monitor/frontend/dist
    destination: /usr/share/nginx/html

hooks:
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 300
      runas: root